name: Debug Workflow

on: [push]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Check files
        run: |
          ls -la
          ls -la .github/
          ls -la .github/workflows/
